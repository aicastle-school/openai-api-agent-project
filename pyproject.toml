[project]
name = "openai-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "openai",
    "pydantic",
    "python-dotenv",
    "python-multipart",
    "uvicorn",
]
